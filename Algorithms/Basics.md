# What is STL Algorithm?

Simple definition:

Algorithms are built-in STL functions used to perform common operations on containers.

## Important Algorithms

These are most important for DSA:

sort()
reverse()
max()
min()
swap()
max_element()
min_element()
binary_search()
count()
find()
accumulate()
next_permutation()

